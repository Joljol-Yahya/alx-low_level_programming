#include <stdio.h>
/**
 * main-Prints digits from the ten to zero
 *
 * Return:Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n == 0; n < 10; n++)
		printf("%d\n", n);	
	printf("\n");

	return (0);

}
