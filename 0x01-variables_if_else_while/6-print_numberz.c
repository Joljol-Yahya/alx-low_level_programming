#include <stdio.h>
/**
 * min-Prints all the numbers
 *
 * Return: Always o (Successu)
 */
int main(void)
{
	char n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}
