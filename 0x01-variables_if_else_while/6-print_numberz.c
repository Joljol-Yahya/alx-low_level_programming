#include <stdio.h>
/**
 * main-Prints all the numbers
 *
 * Return: Always o (Successu)
 */
int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n);
	}

	putchar('\n');

	return (0);
}
